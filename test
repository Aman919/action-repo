from flask import Flask 

app = Flask(__name__)

@app.route('/')
def app_init():
  value=False
  if value == True:
    delta = "You can do it buddy!!"
    return 'You gonna pass this test {}'.format(delta)
  else:
    return 'Don\'t worry'


if __name__ == '__main__':
  app.run(debug=True)
